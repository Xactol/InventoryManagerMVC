@model WebApplication3.ViewModels.Employee.NewEmployeeViewModel
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h1> This is the delete page of employee   </h1>

@using (Html.BeginForm("Remove", "Employee"))
{
   <div>
        <div class="form-group">
            @Html.LabelFor( m => m.Email)
            @Html.TextBoxFor( m => m.Email)
        </div>
        <button type="submit" class="btn btn-primary">Remove employee</button>
  </div>
}
